{
  "name": "cntms",
  "version": "1.0.0",
  "description": "CN Transport Management System - Return Goods (RG) management for WH08",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,red \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "start": "node backend/src/server.js",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "render-build": "npm install && npm --prefix backend install --omit=dev"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
