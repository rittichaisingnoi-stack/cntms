# Claude Code Instructions: Order CN Management System

เอกสารนี้ใช้เป็นไกด์ไลน์สำหรับ Claude Code ในการพัฒนาหรือแก้ไขระบบ Order CN

## 1. ข้อมูลทั่วไปของโปรเจค
- **เป้าหมาย**: ระบบจัดการ Order CN ตั้งแต่เริ่มต้นจนสินค้าเข้าคลัง
- **Tech Stack แนะนำ**: Frontend สมัยใหม่ (React/Vue/Next.js), Tailwind CSS (ถ้าจำเป็นต้องใช้ ให้ใช้โทนสี Blue/Red), Backend ตามที่เหมาะสมกับโปรเจคปัจจุบัน

## 2. สเปคแต่ละหน้า (Page Specifications) — Flow ใหม่ 3 บทบาท

สถานะ order: รอจัดพื้นที่ → มอบหมายแล้ว → รับสินค้าแล้ว → นำกลับคลังแล้ว → ปิดงาน

### 2.1 หน้า Supervisor
- **Login**: สำหรับหัวหน้า
- **กติกาจัดพื้นที่อัตโนมัติ (Priority Rules)**: ผูก Vendor กับค่าของ order โดยระบบไล่จับคู่จากเจาะจงไปกว้าง: **Ship To Code → Sold To → Sold To Code → เขต**
- **Upload Excel** (รายงานสรุป RG เช่น `All_RG.xlsx`): ระบบลบข้อมูลซ้ำ แล้ว **Auto Assign ให้ Vendor** ตามกติกา — order ที่ไม่ตรงกติกาใดเลย ระบบแจ้งเตือนว่า "มีร้านใหม่" และไปค้างที่หน้า **ออเดอร์รอจัดพื้นที่**
- **ออเดอร์รอจัดพื้นที่**: แก้ได้ 2 ทาง — เพิ่มกติกาแล้วกด **Re-assign** หรือเลือก Vendor เองทีละรายการ (assign มือ)
- **Manage Users / Edit Order CN / Dashboard + Export**: เหมือนเดิม
- **Table KPI**: สรุปเฉลี่ยรายวันแยกตาม Vendor + เจาะดูรายออเดอร์ + Export
  - ช่วง 1: รับสินค้าแล้ว − มอบหมายแล้ว · ช่วง 2: นำกลับคลังแล้ว − รับสินค้าแล้ว · ช่วง 3: ปิดงาน − รับสินค้าแล้ว
  - เฉลี่ยเกิน 3 วันขึ้นสีแดง

### 2.2 หน้า Vendor
- **Login**: สำหรับ Vendor
- **งานที่ได้รับมอบหมาย**: ดูงาน + พิมพ์ใบงาน
- **กรอกวันที่จริง (ทำเสร็จแล้วค่อยกรอก)**: วันที่รับสินค้า และวันนำสินค้ากลับคืนคลัง (Key มือรายใบ/ทีละหลายใบ หรือ Load/Upload Excel template)
- **KPI**: เห็นเฉพาะงานของตัวเอง

### 2.3 หน้า GR
- **Login**: สำหรับทีมคลัง (Goods Receipt)
- **Upload file รับสินค้าเข้าระบบ** (`ReportRG-*.xlsx`): จับคู่ด้วย **เลขที่ RG** — แถวที่มี **วันที่สร้าง Doc. WH** = สินค้าเข้าคลังแล้ว → ปิดงานอัตโนมัติ
- **Dashboard Monitor**: ดูออเดอร์ที่กำลังเข้ามา (สถานะนำกลับคลังแล้ว)

> บทบาท Admin TR และ Transport (คนขับรถ) ถูกตัดออกจากระบบแล้ว

## 3. กฎการเขียนโค้ดสำหรับโปรเจคนี้
- ให้ปฏิบัติตาม Brand Tone และ Ironclad Rules ที่ระบุไว้ใน `brand.md` เสมอ
- เน้นการแบ่ง Component ย่อยๆ ให้ชัดเจน
- ใช้ Responsive Design เสมอ (โดยเฉพาะหน้า Transport ต้องเป็น Mobile-First)
- ระวังเรื่อง Security ของ API ให้ผูกกับ Role ของผู้ใช้งานอย่างเคร่งครัด
